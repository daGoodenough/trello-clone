import React from 'react';

const BoardList = () => {
  return (
    <div>
      BoardList
      {/* map through boards in the organization and render them using the BoardItem component */}
    </div>
  );
}

export default BoardList;
